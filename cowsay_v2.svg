<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 200" width="100%" height="200">
  <defs>
    <!-- Phosphor Glow Filter -->
    <filter id="neon-glow" x="-20%" y="-20%" width="140%" height="140%">
      <feGaussianBlur stdDeviation="2.5" result="blur"/>
      <feMerge>
        <feMergeNode in="blur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>

    <!-- Scanline Gradient -->
    <linearGradient id="scanline-grad" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stop-color="#00FF99" stop-opacity="0" />
      <stop offset="50%" stop-color="#00FF99" stop-opacity="0.10" />
      <stop offset="100%" stop-color="#00FF99" stop-opacity="0" />
    </linearGradient>
  </defs>
  <style>
    @keyframes scanline {
      0% { transform: translateY(-200px); }
      100% { transform: translateY(200px); }
    }
    .bg { fill: #07090e; }
    .text-green { font-family: 'Consolas', 'Courier New', monospace; font-size: 13.5px; fill: #00FF99; }
    .text-neon { font-family: 'Consolas', 'Courier New', monospace; font-size: 13.5px; fill: #00FF99; filter: url(#neon-glow); }
    .text-sub { font-family: 'Consolas', 'Courier New', monospace; font-size: 13px; fill: #00FF99; opacity: 0.7; }
    
    .window-dot { stroke: #00FF99; stroke-width: 1.5; fill: none; opacity: 0.5; }
    .window-dot-filled { fill: #00FF99; opacity: 0.8; }
    .border-line { stroke: #00FF99; stroke-width: 1.5; stroke-opacity: 0.25; }
    .scanline { fill: url(#scanline-grad); animation: scanline 8s linear infinite; }
  </style>

  <!-- Background -->
  <rect width="100%" height="100%" class="bg" />
  
  <!-- Terminal Border Window frame (Fully Closed, 800px width matching all panels) -->
  <path d="M 10,10 L 790,10 L 790,190 L 10,190 Z" fill="none" class="border-line" />
  <path d="M 10,40 L 790,40" class="border-line" />

  <!-- Terminal Window Controls -->
  <circle cx="30" cy="25" r="5" class="window-dot-filled" />
  <circle cx="48" cy="25" r="5" class="window-dot" />
  <circle cx="66" cy="25" r="5" class="window-dot" />
  
  <text x="400" y="29" text-anchor="middle" class="text-sub">session: fortune | cowsay</text>

  <!-- Cowsay terminal output (Lines separated explicitly to avoid HTML whitespace collapse) -->
  <g xml:space="preserve" class="text-green">
    <text x="230" y="65"> _________________________________________</text>
    <text x="230" y="80">/ Security is not a product, but a        \</text>
    <text x="230" y="95">\ process. - Bruce Schneier               /</text>
    <text x="230" y="110"> -----------------------------------------</text>
    <text x="230" y="125">        \   ^__^</text>
    <text x="230" y="140">         \  (oo)\_______</text>
    <text x="230" y="155">            (__)\       )\/\</text>
    <text x="230" y="170">                ||----w |</text>
    <text x="230" y="185">                ||     ||</text>
  </g>

  <!-- CRT Scanline overlay -->
  <rect width="100%" height="200" class="scanline" pointer-events="none" />
</svg>
